properties:
  message:
    properties:
      order:
        properties:
          status:
            type: string
            enum:
              - COMPLETED
              - ACTIVE
              - CANCELLED
              - SOFT_CANCEL
        required:
          - status
